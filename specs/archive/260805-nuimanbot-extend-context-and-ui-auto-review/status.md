# Status: NuimanBot Extend Context & UI — Auto-Review Fix Pass

**Created:** 2026-08-05
**Feature directory:** `specs/260805-nuimanbot-extend-context-and-ui-auto-review/`
**Source PRD:** 19 findings (5 P0, 9 P1, 5 P2) from a code review of `worktree-splendid-petting-falcon`.

## Overall Progress

| Phase | Workstream(s) | Status | Progress |
|---|---|---|---|
| Phase 0 — Spec Creation | — | In Progress | Spec directory + phase files initialized |
| Phase A — Restart & retention sweeps | FR-R2, FR-R3, FR-R9 | Complete | 3/3 findings |
| Phase B — Filesystem layering & sandboxing | FR-R5, FR-R6, FR-R18, FR-R13 | Complete | 4/4 findings |
| Phase C — Chore parity | FR-R8 | Complete | 1/1 findings |
| Phase D — Deferred agent-facing surfaces | FR-R1, FR-R4, FR-R7 | Complete | 3/3 findings |
| Phase E — Live-update & observability plumbing | FR-R10, FR-R19 | Complete | 2/2 findings |
| Phase F — Settings & execution edge cases | FR-R11, FR-R12, FR-R14, FR-R15 | Complete | 4/4 findings |
| Phase G — Consistency polish | FR-R16, FR-R17 | Complete | 2/2 findings |

**Overall: 19/19 findings closed. All workstreams complete.**

**Correction (2026-08-05):** FR-R7 (Memories `ownerUserID`→`ConversationID` mapping, P1) was present in the PRD/`research.md`/`architecture.md` but never assigned to a workstream in the original table (the table summed to 18, not 19). Added to Phase D as Task D.3 — see `tasks.md` for rationale.

Phase lettering (A–G) matches the PRD's own "Fix-Pass Execution Guidance" workstream table exactly — this spec does not re-derive a different grouping. See `plan.md` and `tasks.md` for the sequencing rules within and across phases (notably: C before A's FR-R9; B's FR-R5 before FR-R6; FR-R18 rebases against FR-R5).

## Phase 0 Task Checklist

- [x] Spec directory created: `specs/260805-nuimanbot-extend-context-and-ui-auto-review/`
- [x] Source PRD moved into spec directory
- [x] `spec.md` populated from PRD (19 findings indexed by priority, NFRs, goals/non-goals, risks)
- [x] Research questions identified (from PRD's Open Questions section) — see `research.md`
- [x] All phase files initialized (spec.md, status.md, research.md, data-dictionary.md, architecture.md, plan.md, tasks.md, implementation-notes.md)
- [ ] `dev-flow:review-spec` run against this spec directory (suggested next step)

## Blockers

None currently. Four Open Questions from the PRD are carried forward as decisions-with-defaults (not blockers) — see `research.md` and `plan.md`; each fix pass proceeds under the stated default absent new information.

## Recent Activity

- 2026-08-05: Phase E complete (both findings, TDD Red-Green-Refactor for each) — **all 19 findings now closed.** FR-R19: centralized `UnviewedRunCount` population into `baseDataFor` (the shared "build BaseData for an authenticated request" path already used by every environment handler), so every authenticated page gets the History notification badge, not just History's own two handlers. `withUnviewedRunCount` gained a `s.historyService == nil` guard since it's now called unconditionally (previously only reachable from handlers that had already checked `historyService != nil` for other reasons). `baseDataFor`'s signature grew a `ctx context.Context` first param — updated at all 16 call sites plus `dashboard_handler.go`'s `getDashboardData` (Dashboard previously called `NewBaseData` directly, bypassing `baseDataFor` entirely, now consolidated into the same shared path). New test `TestNotificationBadge_AppearsOnNonHistoryPages` proves the badge now renders on Chats/Jobs/Dashboard/History; `TestNotificationBadge_AbsentWhenHistoryServiceUnconfigured` proves the centralization doesn't panic when HistoryService is unset (a real risk given many existing handler tests construct servers with only one service wired). FR-R10: new `internal/adapter/web/static/run-events.js` — dependency-free vanilla JS, connects to the existing `/ws` endpoint, listens for `run_status`/`run_log`/`notification_badge` events (the wire shape `websocket_handler.go`'s `RunEvent` already defines) and patches the relevant DOM element without a page reload. Wired into `job_detail.html`/`chore_detail.html`/`run_detail.html` via a `<script src="/static/run-events.js" defer>` tag plus `data-source-type`/`data-source-id` (Job/Chore, matched by SourceType+SourceID since these pages don't know their current Run's ID ahead of time) or `data-run-id` (Run detail, matched directly) attributes on `<body>`; target elements got stable IDs (`#job-status`, `#chore-status` — new, since Chore's detail page had no live-run-status element before this — `#run-status`, `#run-log`). `nav.html`'s badge `<span>` always renders now (toggled via a `hidden` class instead of a `{{if}}` block) so the script has a stable `#notification-badge` element to update live, matching FR-R19's page-load population. **Manual/live-browser verification was not performed** — no browser is available in this environment; `TestRunEventsJS_ServedAndWiredIntoDetailPages` structurally verifies the script is served, contains real `WebSocket` client code and all three event-type strings, and that each of the three target detail pages includes the script tag plus every DOM anchor the script depends on — documented as a Deviation from Plan rather than left silent. **Gotcha hit during implementation:** the test file was initially named `run_events_js_test.go` and silently excluded from the build — Go's filename-based build-constraint rules treat a `_js` suffix immediately before `_test.go` as an implicit `GOOS=js` constraint (the `js/wasm` platform), so the file compiled for no target this repo builds for and `go test -list` showed zero matches with no error. Renamed to `run_events_client_test.go` to fix. Touched packages: `internal/adapter/web`. `gofmt -l`/`go vet`/`golangci-lint run` clean; full repo `go build ./...`/`go test ./...` green.
- 2026-08-05: Phase D complete (all 3 findings, TDD Red-Green-Refactor for each). FR-R4 (P0, no documentation-only fallback accepted): implemented Memory as the per-item chat template reference implementation — `memories.Service.AskAboutCell` (new `memories.LLMService` minimal single-turn interface, structurally satisfied by `domain.LLMService`, deliberately NOT the full `internal/usecase/chat` orchestration engine) answers a question grounded only in one memory cell's own content via a new `POST /admin/memories/{id}/ask` route; the answer renders inline on the same detail page (no persisted per-item chat history — a documented, minimal-template scope choice). Wired in `cmd/nuimanbot/extended_context.go` via `memories.WithLLM(app.LLMService)`/`WithLLMDefaults` sharing `cfg.LLM.DefaultModel`. The other three items (Job/Chore/Run) explicitly say "Chat interface coming soon." on their detail pages (Job's notice already existed; added to `chore_detail.html`/`run_detail.html` to match) rather than silently omitting the affordance, per the finding's acceptance criteria. FR-R1 (P1): per the PRD's own Non-Goals ("Wiring the live Chats reply loop... [is a] separately-scoped follow-up effort, not a target of this pass"), took the documented-deferral path — `chat_detail.html` now shows a visible amber notice that automatic replies aren't yet available, so the gap is disclosed rather than silent. FR-R7 (P1, the orphaned finding — see the Progress Summary correction in `tasks.md`): traced the real CLI gateway end-to-end (`internal/adapter/gateway/cli/gateway.go` -> `chat.Service.getConversationID` -> `memoryv2.MemoryCuratorService.ExtractCells`) and proved memory cells are actually keyed `"<platform>:<platformUID>"` (e.g. `"cli:cli_user"` — literally every CLI interaction today, since the CLI gateway hardcodes one placeholder PlatformUID for all users), never the web-admin session Username `memories.Service` receives as `ownerUserID`; further confirmed `internal/adapter/web`'s login/session system never references `domain.UserRepository`/`domain.User` at all, so there is no existing identity bridge in this codebase to look up even if attempted. New integration test `TestFR_R7_ConversationIDMapping_TracesRealCLIGatewayFormat` (`internal/usecase/memories/conversation_id_mapping_test.go`) creates a cell via the real curator path (real file-backed repositories, not a synthetic `ConversationID = username` fixture) and proves the current mapping under-shows it. Package doc comment in `memories/service.go` updated from "ASSUMPTION, needs reviewer confirmation" to a confirmed, evidenced finding; `memories.html` carries a visible notice that the view may not show everything. Full fix (a real web-account <-> domain.User/platform-identity bridge, plus fixing the CLI gateway's own single-shared-placeholder identity) is out of scope for this pass, same class of deferral as FR-R1 — documented in `implementation-notes.md`'s Deviations from Plan. Touched packages: `internal/usecase/memories` (100% coverage), `internal/adapter/web`, `cmd/nuimanbot`. `gofmt -l`/`go vet`/`golangci-lint run` clean on all touched files; `go test ./internal/usecase/memories/... ./internal/adapter/web/... ./cmd/nuimanbot/...` green.
- 2026-08-05: Phase G complete (both findings, TDD Red-Green-Refactor for each). FR-R16: `dashboard.html`, `bots.html`, `users.html`, and `confirmations.html` now include `{{template "nav" .}}` immediately after `<body>`, matching the six newer environment pages. Since `bot_handler.go`/`user_handler.go`/`confirmation_handler.go` previously passed bare anonymous-struct template data (no `*BaseData`), nav's fields (`.IsAuthenticated`, `.ActivePage`, `.CurrentUser`, `.UnviewedRunCount`) would have failed to resolve — all three handlers were updated to embed `*BaseData` via the existing `s.baseDataFor(user, title, activePage)` helper (the same pattern the Jobs/Chats/etc. handlers already use), so the sidebar renders correctly with the current page highlighted and the user menu populated. Verified via a new `TestLegacyAdminPagesIncludeNavSidebar` test plus an ad-hoc (removed after use) sanity check confirming the Bots nav link gets the active-page highlight class. FR-R17: added `ReadLog`/`ReadResults` to `domain.RunRepository`, implemented in `FileRunRepository` — `ReadLog` reads through the repository's own fsguard-confined per-owner log path (the path `AppendLog` actually writes to; `Run.LogPath` is a dead field no Executor ever populates), `ReadResults` reads `Run.ResultsPath` directly (server-set by the Executor, outside this repository's own storage root, so not re-resolvable through its own fsguard base — matches the finding's own "not currently exploitable" characterization). Both new methods pass through `usecase/history.Service` and the `web.HistoryService` interface; `history_handler.go`'s `handleHistoryDetail` now calls `s.historyService.ReadLog`/`ReadResults` instead of the removed `readFileContentOrEmpty`/raw `os.ReadFile`, and no longer imports `"os"`. `MockHistoryService` (test double) grew matching `ReadLog`/`ReadResults` methods that preserve the old file-path-based test behavior so all pre-existing History handler tests pass unmodified. Touched packages: `internal/adapter/web`, `internal/domain`, `internal/infrastructure/storage`, `internal/usecase/history`. Coverage: `internal/adapter/web` 81.8%, `internal/infrastructure/storage` 83.6%, `internal/usecase/history` 92.0%. `go build`/`gofmt -l`/`golangci-lint run` all clean on touched packages; full `go test` on all touched + rippled packages (web, domain, infrastructure/storage, usecase/history, usecase/jobs, usecase/chores) green. One transient build/test failure was observed mid-run from a concurrently-editing agent's in-flight save to `memories_handler.go`/`settings_handler.go` (files this workstream never touches) — resolved on retry, not caused by this work.
- 2026-08-05: Phase F complete (all 4 findings, TDD Red-Green-Refactor for each). FR-R11: Settings template now displays Bind address (disabled input) and clarifies Allowlist/Bind-address are config-file-only, plus a note that Network access mode only affects the allowlist-check middleware, not the actual listener bind address. FR-R12: `StubExecutor` now takes `JobRepository`/`ChoreRepository`/`ProjectRepository`; before executing, it resolves the source Job/Chore's `ContextType`/`ContextID` and, if Project-scoped, verifies the Project still exists via `ProjectRepository.GetProject`, failing the Run with `Error = "referenced Project no longer exists"` if not (exact Edge Case #2 wording). `cmd/nuimanbot/extended_context.go`'s `NewStubExecutor` call updated to pass `app.JobRepo`/`app.ChoreRepo`/`app.ProjectRepo`. FR-R14: `Job.IsQueueable()` removed (with its 4 dedicated tests in `job_test.go`, which is now deleted) — confirmed via grep it had zero non-test callers, and there is no re-run/re-enqueue path anywhere in the codebase for its guard logic to apply to (`CreateJob` always builds a fresh, trivially-queueable Job). Decision documented in a comment in `jobs/service.go` near `DeleteJob`. FR-R15: added `domain.MaxWorkerPoolSize = 256` and a `Validate()` upper-bound check; `settings_handler.go`'s POST handler now rejects values above the bound with a flash error, same pattern as the existing `<= 0` check. Touched packages: `internal/adapter/web`, `internal/infrastructure/scheduler`, `internal/domain`, `internal/usecase/jobs`, `cmd/nuimanbot`. Coverage: scheduler 80.8%, domain 97.9%, usecase/jobs 92.2%. `go build`/`go vet`/`golangci-lint run`/`gofmt -l` all clean on touched files. Full `internal/adapter/web` package `go test` shows one unrelated failure (`TestHandleMemoryAsk_ServiceErrorRendersInline`) in `memories_handler_test.go`/`memories_handler.go`, which Workstream F never touched — owned by a different concurrently-editing agent in this shared worktree; all Settings-specific tests pass.
- 2026-08-05: Task C.1 (FR-R8) complete — `chores.Service.DeleteChore` now soft-marks `PendingDeletion` when a Queued/Running Run exists for the Chore (derived via `RunRepository.ListRuns`, since unlike Job, Chore has no Status field of its own), mirroring `jobs.Service.DeleteJob`. `NewService` gained a `runs domain.RunRepository` param; `cmd/nuimanbot/extended_context.go` wiring updated. TODO comment removed and replaced with a "sweep integration deferred" note referencing FR-R9. Coverage: chores package 90.2% → 93.4%. All quality gates green.
- 2026-08-05: Spec directory created from `nuimanbot-extend-context-and-ui-auto-review-PRD.md` via `dev-flow:create-spec`. PRD moved into this directory. Phase files initialized directly from the PRD's own workstream/sequencing structure (Fix-Pass Execution Guidance table) rather than a generic placeholder structure, per explicit pipeline instruction — the PRD's grouping is detailed and specific enough to translate directly.

**MANDATORY REMINDER:** Update this file after every task/phase completion during implementation. This is the single source of truth for fix-pass progress.
